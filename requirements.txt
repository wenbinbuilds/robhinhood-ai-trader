pytest>=8.0,<9.0
jsonschema>=4.23,<5.0
mcp==2.2.0
keyring>=25.6,<26.0
# Offline-only RL research. Imports are lazy so the core shadow runner remains
# fail-safe if an operator intentionally installs only the non-RL dependencies.
numpy>=2.0,<3.0
gymnasium>=1.0,<2.0
stable-baselines3>=2.6,<3.0
